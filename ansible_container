FROM ubuntu
LABEL maintainer="warr.doge@gmail.com"
RUN apt-get update && apt-get install -y python3 && apt-get install -y python3-pip && pip3 install ansible && apt-get install -y aws-cli
COPY ansible ./
WORKDIR /ansible
ENTRYPOINT ansible-playbook playbook.yml